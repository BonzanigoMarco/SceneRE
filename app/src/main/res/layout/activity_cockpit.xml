<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".activities.CockpitActivity">

    <android.support.v7.widget.Toolbar
        android:id="@+id/cockpit_toolbar"
        android:layout_width="match_parent"
        android:layout_height="?attr/actionBarSize"
        style="@style/HeaderBar"
        android:elevation="4dp"
        />

    <TextView
        android:id="@+id/cockpit_text_label"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center_horizontal"
        android:textColor="@color/sreWhite"
        android:background="@color/colorPrimary"
        android:text="Cockpit"
        app:layout_constraintTop_toBottomOf="@id/cockpit_toolbar"/>

    <uzh.scenere.views.SwipeButtonScrollView
        android:id="@+id/cockpit_scroll"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toBottomOf="@id/cockpit_text_label">
        <LinearLayout
            android:id="@+id/cockpit_linear_layout_holder"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:orientation="vertical">
        </LinearLayout>

    </uzh.scenere.views.SwipeButtonScrollView>

    <LinearLayout
        android:id="@+id/cockpit_layout_scenario_management"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/sreWhite"
        app:layout_constraintBottom_toBottomOf="parent">

        <Button
            android:id="@+id/cockpit_button_scenario_management"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_margin="@dimen/margin"
            android:onClick="onNavigationButtonClicked"
            android:gravity="center_horizontal"
            android:text="Select"
            tools:ignore="OnClick"/>
    </LinearLayout>



        <!--app:theme="@style/ActionBarThemeOverlay"-->
        <!--app:popupTheme="@style/ActionBarPopupThemeOverlay"-->

</android.support.constraint.ConstraintLayout>